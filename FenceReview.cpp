#include<stdio.h>
#include<iostream>
#include<vector>

using namespace std;

int N;
int search(vector<int> &a, int left, int right){
	
}

int main(void){
	int c, temp;
	scanf("%d",&c);
	for(int i = 0 ; i < c; i++){
		scanf("%d",&N);
		vector<int> input;
		for(int j = 0; j < N; j++){
			scanf("%d",&temp);
			input.push_back(temp);
		}
	}
}